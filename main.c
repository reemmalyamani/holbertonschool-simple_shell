#include "shell.h"

/**
 * main - simle shell enters HERE
 * Return: 0 on success
 */
int main(void)
{
    /* Start the shell loop: (prompt user -> read input -> execute -> repeat) */
    shell_loop();

    return (0);
}

